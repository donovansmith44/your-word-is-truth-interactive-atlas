# Curated landmark labels for the World map's "actual good and informative
# map" feature (user feedback: "we should be labeling landmarks such as
# rivers and mountains"). Rendered always-visible, non-interactive, styled
# by `kind` per design-direction.md's Atlas plate detail: water names in
# italic serif (--lapis-leaning), mountain/region names in letterspaced
# small caps (--ink-soft) -- both a visual step below place markers'
# labels, so scripture's places stay the foreground.
#
# `kind` must be exactly one of "water" | "mountain" | "region"
# (atlas-etl's validate::run_landmarks enforces this). `lat`/`lon` must
# fall inside the biblical-world clip bbox (lat 7.6..48.9, lon -10.9..71.4
# -- same constants as client/wwwroot/js/map.js's BIBLICAL_WORLD_BOUNDS and
# atlas-etl's borders::BIBLICAL_WORLD_BBOX).
#
# Every entry below reuses a real geocoded coordinate from
# data/compiled/places.json (cited by slug in each entry's comment) rather
# than inventing coordinates -- per the batch brief's "reuse coordinates
# from data/compiled/places.json where a matching place exists". A couple
# of entries deliberately cite a DIFFERENT (but still real, still
# same-named-or-directly-relevant) geocoded record than the most obvious
# match, specifically to avoid stacking multiple landmark labels on the
# exact same pixel when several biblical names geocode to one shared
# point in this dataset (noted case by case below).

# --- water (8) ---------------------------------------------------------

[[landmark]] # places.json slug "jordan"
name = "Jordan River"
kind = "water"
lat = 31.761389
lon = 35.558333

[[landmark]] # places.json slug "great-sea" (KJV name for the Mediterranean, e.g. Num 34:6)
name = "The Great Sea"
kind = "water"
lat = 31.804819
lon = 34.170350

[[landmark]] # places.json slug "sea-of-galilee"
name = "Sea of Galilee"
kind = "water"
lat = 32.818906
lon = 35.590033

[[landmark]] # places.json slug "salt-sea" (KJV name for the Dead Sea)
name = "Dead Sea"
kind = "water"
lat = 31.500000
lon = 35.500000

[[landmark]] # places.json slug "red_sea_986" (the plain "Red Sea" geocoded record,
# centrally within the sea itself rather than one specific gulf/crossing point)
name = "Red Sea"
kind = "water"
lat = 19.000000
lon = 39.500000

[[landmark]] # places.json slug "nile"
name = "Nile"
kind = "water"
lat = 31.465300
lon = 30.366700

[[landmark]] # places.json slug "euphrates"
name = "Euphrates"
kind = "water"
lat = 31.004300
lon = 47.442000

[[landmark]] # places.json slug "tigris"
name = "Tigris"
kind = "water"
lat = 31.004300
lon = 47.442100

# --- mountain (8) --------------------------------------------------------

[[landmark]] # places.json slug "mount-sinai"
name = "Mount Sinai"
kind = "mountain"
lat = 28.539722
lon = 33.973333

[[landmark]] # places.json slug "horeb_576" (a separate geocoded "Horeb" identification
# from "mount-horeb", chosen so its label doesn't sit exactly on top of Mount
# Sinai's -- the two are traditionally identified as the same or adjoining massif)
name = "Mount Horeb"
kind = "mountain"
lat = 28.587710
lon = 33.921682

[[landmark]] # places.json slug "mount-carmel"
name = "Mount Carmel"
kind = "mountain"
lat = 32.672500
lon = 35.023333

[[landmark]] # places.json slug "ararat"
name = "Mount Ararat"
kind = "mountain"
lat = 39.702761
lon = 44.300381

[[landmark]] # places.json slug "mount-of-olives"
name = "Mount of Olives"
kind = "mountain"
lat = 31.777946
lon = 35.245686

[[landmark]] # places.json slug "mount-hermon"
name = "Mount Hermon"
kind = "mountain"
lat = 33.400000
lon = 35.850000

[[landmark]] # places.json slug "mount-gilboa"
name = "Mount Gilboa"
kind = "mountain"
lat = 32.433889
lon = 35.414444

[[landmark]] # places.json slug "mount-nebo"
name = "Mount Nebo"
kind = "mountain"
lat = 31.766667
lon = 35.750000

# --- region (3) ----------------------------------------------------------

[[landmark]] # places.json slug "sinai_1098" (the generic "Sinai" geocoded record --
# chosen over "wilderness-of-sinai", which coincides exactly with mount-sinai's
# point in this dataset, so the region label doesn't stack on the mountain's)
name = "Wilderness of Sinai"
kind = "region"
lat = 29.500000
lon = 34.000000

[[landmark]] # places.json slug "arabah"
name = "Arabah"
kind = "region"
lat = 30.416944
lon = 35.151389

[[landmark]] # places.json slug "negeb" (spelled "Negeb" in the source data;
# "Negev" is the standard modern English spelling, used here as the display name)
name = "Negev"
kind = "region"
lat = 31.244722
lon = 34.840833
